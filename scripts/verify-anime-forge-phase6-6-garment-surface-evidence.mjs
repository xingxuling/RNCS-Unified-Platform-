import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {rootHash} from '../packages/world/native-character-morphogenesis-runtime/src/canonical.mjs';
const repoRoot=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),arg=process.argv.indexOf('--evidence'),dir=arg>=0?path.resolve(process.argv[arg+1]):path.join(repoRoot,'evidence/anime-forge-phase6-6-native-drawing-infrastructure-v0.1'),file=path.join(dir,'garment-surface-evidence.json'),failures=[];
if(!fs.existsSync(file))failures.push('GARMENT_SURFACE_EVIDENCE_MISSING');
if(!failures.length){const data=JSON.parse(fs.readFileSync(file,'utf8'));if(data.format!=='rncs.phase6-6-garment-surface-evidence.v0.1')failures.push('GARMENT_SURFACE_EVIDENCE_FORMAT_INVALID');if(data.status!=='passed')failures.push('GARMENT_SURFACE_EVIDENCE_STATUS_FAILED');if(data.large_form_authority!=='GarmentSurfaceDrawingGuide'||data.secondary_motion_authority!=='DrawingMeshCage')failures.push('GARMENT_SURFACE_AUTHORITY_INVALID');if(data.entries?.length!==3)failures.push('GARMENT_SURFACE_VIEW_COUNT_INVALID');if(!data.entries?.every(entry=>entry.pass&&entry.canonical_mesh_root&&entry.surface_weighting_root&&entry.mesh_silhouette_guide_root&&entry.garment_surface_drawing_guide_root&&entry.garment_attachment_root&&entry.minimum_clearance_area_ratio>1&&entry.large_form_authority==='GarmentSurfaceDrawingGuide'&&entry.secondary_motion_authority==='DrawingMeshCage'&&entry.legacy_drawing_ir_root&&entry.candidate_drawing_ir_root&&entry.legacy_drawing_ir_root!==entry.candidate_drawing_ir_root&&entry.comparisons?.length===6&&entry.comparisons.every(item=>item.changed&&item.bound&&item.garment_surface_guide_root)&&entry.cage_secondary_motion?.secondary_motion_active===true&&entry.cage_secondary_motion?.triangle_inversions===0))failures.push('GARMENT_SURFACE_BINDING_INVALID');if(data.human_visual_acceptance!=='pending')failures.push('GARMENT_SURFACE_HUMAN_GATE_INVALID');if(data.evidence_root!==rootHash({...data,evidence_root:''}))failures.push('GARMENT_SURFACE_EVIDENCE_ROOT_MISMATCH');}
console.log(JSON.stringify({format:'rncs.phase6-6-garment-surface-verification.v0.1',passed:failures.length===0,failures,evidence_dir:dir},null,2));if(failures.length)process.exit(1);
