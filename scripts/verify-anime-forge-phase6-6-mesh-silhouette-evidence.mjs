import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {rootHash} from '../packages/world/native-character-morphogenesis-runtime/src/canonical.mjs';
const repoRoot=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),arg=process.argv.indexOf('--evidence'),dir=arg>=0?path.resolve(process.argv[arg+1]):path.join(repoRoot,'evidence/anime-forge-phase6-6-native-drawing-infrastructure-v0.1'),file=path.join(dir,'mesh-silhouette-evidence.json'),failures=[];
if(!fs.existsSync(file))failures.push('MESH_SILHOUETTE_EVIDENCE_MISSING');
if(!failures.length){const data=JSON.parse(fs.readFileSync(file,'utf8'));if(data.format!=='rncs.phase6-6-mesh-silhouette-evidence.v0.4')failures.push('MESH_SILHOUETTE_EVIDENCE_FORMAT_INVALID');if(data.status!=='passed')failures.push('MESH_SILHOUETTE_EVIDENCE_STATUS_FAILED');if(data.required_primary_paths!==10||data.required_semantic_groups?.length!==10)failures.push('MESH_SILHOUETTE_REQUIRED_GROUP_COUNT_INVALID');if(data.entries?.length!==3)failures.push('MESH_SILHOUETTE_VIEW_COUNT_INVALID');if(!data.entries?.every(entry=>entry.canonical_mesh_root&&entry.weighted_mesh_root&&entry.surface_weighting_root&&entry.posed_mesh_root&&entry.visibility_root&&entry.mesh_silhouette_guide_root&&entry.legacy_drawing_ir_root&&entry.candidate_drawing_ir_root&&entry.legacy_drawing_ir_root!==entry.candidate_drawing_ir_root&&entry.changed_bound_primary_path_count===10&&Object.keys(entry.group_part_counts??{}).length===10&&entry.comparisons?.length===10&&entry.comparisons.every(item=>item.changed&&item.bound&&item.mesh_silhouette_guide_root&&item.mesh_silhouette_group&&item.mesh_silhouette_part_root)&&Array.isArray(entry.extra_visible_parts)&&entry.extra_visible_parts.every(item=>item.group&&item.guide_root&&item.part_root&&item.path_root)))failures.push('MESH_SILHOUETTE_ROOT_PATH_OR_MULTIPART_BINDING_INVALID');if(data.pose_delta?.changed!==true||!data.pose_delta?.neutral_guide_root||!data.pose_delta?.action_guide_root||data.pose_delta.neutral_guide_root===data.pose_delta.action_guide_root||!data.pose_delta?.neutral_part_counts||!data.pose_delta?.action_part_counts)failures.push('MESH_SILHOUETTE_POSE_DELTA_INVALID');if(data.root_chain?.stable_across_views!==true||!data.root_chain?.canonical_mesh_root||!data.root_chain?.surface_weighting_root)failures.push('MESH_SILHOUETTE_AUTHORITY_ROOT_DRIFT');if(data.human_visual_acceptance!=='pending')failures.push('MESH_SILHOUETTE_HUMAN_GATE_INVALID');if(data.evidence_root!==rootHash({...data,evidence_root:''}))failures.push('MESH_SILHOUETTE_EVIDENCE_ROOT_MISMATCH');}
console.log(JSON.stringify({format:'rncs.phase6-6-mesh-silhouette-verification.v0.4',passed:failures.length===0,failures,evidence_dir:dir},null,2));if(failures.length)process.exit(1);
