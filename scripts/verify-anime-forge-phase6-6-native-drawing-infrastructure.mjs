import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {rootHash} from '../packages/world/native-character-morphogenesis-runtime/src/canonical.mjs';
const repoRoot=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),arg=process.argv.indexOf('--evidence'),dir=arg>=0?path.resolve(process.argv[arg+1]):path.join(repoRoot,'evidence/anime-forge-phase6-6-native-drawing-infrastructure-v0.1'),read=name=>JSON.parse(fs.readFileSync(path.join(dir,name),'utf8')),required=['static-gates.json','frame-manifest.json','episode.wav','episode.mp4','ffprobe-report.json','backend-receipt.json','phase-status.json','evidence-ledger.json','evidence-summary.json'],failures=[];for(const name of required)if(!fs.existsSync(path.join(dir,name)))failures.push(`MISSING:${name}`);if(!failures.length){const gates=read('static-gates.json'),frames=read('frame-manifest.json'),backend=read('backend-receipt.json'),status=read('phase-status.json'),ledger=read('evidence-ledger.json'),summary=read('evidence-summary.json');if(gates.status!=='passed'||gates.entries?.length!==3)failures.push('STATIC_VECTOR_GATES_FAILED');if(!gates.entries.every(e=>e.svg?.sha256&&e.vector_png?.sha256&&e.phase6_5_baseline?.sha256))failures.push('STATIC_AB_EVIDENCE_INCOMPLETE');if(frames.frame_count!==120||frames.width!==1280||frames.height!==720||frames.frames?.length!==120)failures.push('FRAME_MANIFEST_INVALID');if(frames.drawing_backend!=='svg-path+cubic-bezier'||frames.raster_backend!=='librsvg')failures.push('VECTOR_BACKEND_CONTRACT_INVALID');if(backend.supersample_factor!==2||backend.external_visual_model!==false)failures.push('BACKEND_RECEIPT_INVALID');if(status.human_visual_acceptance!=='pending'||status.creative_production_review!=='pending-human-review')failures.push('HUMAN_GATE_MUST_REMAIN_PENDING');if(summary.mp4_sha256!==ledger.media_sha256)failures.push('MEDIA_SHA_MISMATCH');if(ledger.ledger_root!==rootHash({...ledger,ledger_root:''}))failures.push('LEDGER_ROOT_MISMATCH');}
console.log(JSON.stringify({format:'rncs.phase6-6-verification.v0.1',passed:failures.length===0,failures,evidence_dir:dir},null,2));if(failures.length)process.exit(1);
