import fs from 'node:fs';import path from 'node:path';import {runBehaviorSequencerWorld} from './runtime.mjs';
const result=await runBehaviorSequencerWorld();const out=path.resolve('artifacts/behavior-sequencer-v011');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'evidence.json'),JSON.stringify(result,null,2)+'\n');const pass=Object.values(result.acceptance).every(Boolean);console.log(JSON.stringify({status:pass?'PASS':'FAIL',artifact:out,...result.acceptance,roots:{quest:result.quest.quest_root,task:result.cooperative_task.task_root,replay:result.replay.replay_root,sequence:result.sequence.sequence_root}},null,2));if(!pass)process.exitCode=1;
