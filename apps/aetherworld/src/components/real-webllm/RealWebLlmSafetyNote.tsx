export function RealWebLlmSafetyNote() {
  return (
    <div className="text-[11px] text-muted-foreground leading-relaxed">
      本地 WebLLM 仅作为辅助层，不能替代计算法、常数与系统宪法；不会声称模拟结果是真实执行，也不会绕过 QA 与能力包安装机制。
    </div>
  );
}
