# STATUS

- Version: `0.2.0-alpha.1`
- Status: Alpha / native packaging and capability-aware build fabric
- Input: Reality Studio Unified Project v0.9
- Targets: Web Release, Web Single, Windows Portable, Windows Native EXE, Android Project, Android APK
- Deterministic target roots: implemented
- Asset content addressing: implemented
- Incremental cache hit: implemented
- File integrity verification: implemented
- Build capability preflight: implemented
- Native Windows GUI EXE: implemented and cross-compiled in release validation
- Windows EXE self-contained payload: implemented
- Windows embedded rendering runtime: not implemented; system Edge/Chrome is used
- Android project one-click scripts: implemented
- Compiled/debug-signed Android APK target: implemented; local Gradle 9.5.1 + Android SDK 35 host build and Windows apksigner verification passed; embedded Large World dynamic WebView candidate also executed on `Rcl_Aether_API35_ATD`; physical/target-device execution remains open
- Deterministic Behavior + RSR spatial build evidence: implemented
- Spatial snapshot, causal delta and runtime manifest outputs: implemented
- TileMap collision/navigation manifest output: implemented when the active scene has a TileMap
- Headless spatial inspection and step endpoints: implemented
- Browser VSR WebGPU executor: implemented for Web Release, single-file, Windows Portable and embedded Android HTML targets; build-time GPU resources plus per-Tick dynamic entity/light bindings, tiled lights, particles, postprocess and Canvas/device-loss fallback are verified
- Browser-native RSR 2D authority bridge: implemented for behavior-bound colliders; browser targets execute the compiled `SpatialEmbodimentWorld`, publish spatial roots/events, and synchronize its body state back into the host frame
- Browser GPU frame now derives dynamic vertex/light buffers from the current BehaviorRuntime state and re-seals its evidence roots per Tick; an explicit Large World candidate also reaches the VSR GLB importer and WebGPU executor in local Chromium, while native rendering and complete device-matrix validation remain open work
- Candidate-only VSR presentation consumer: implemented for explicit World Body and generic Large World presentation candidates; Build evidence executes shared VSR asset-catalog resolution (`requested=4`, `missing=0`), and the Large World GLB candidate bundle now has a web-release content-addressed payload manifest, browser VSR byte loading/hash verification (`catalog=36`, active request/ready=`33/33`, missing/failed=`0/0`), explicit 11-record mesh replacement bindings, generic VSR residency transition receipt, local Chromium WebGPU draw evidence (`drawCalls=11`, `triangles=402`, `submitted=true`, `deviceLost=false`), a locally built/apksigner-verified debug APK, and an embedded Android dynamic working-set candidate; physical/target-device GPU, quota/eviction/performance curves, network, and production asset service remain open
- Android Emulator WebView candidate: the generated Large World APK cold-started twice in `Rcl_Aether_API35_ATD`; the embedded page reached `readyState=complete`, RSR and 3D frame verification stayed true, 33/33 payloads and 11 bindings were ready, and Canvas2D fallback rendered; the baseline candidate used `file:///android_asset/index.html`, while the current cache-capable host uses the verified `https://rncs.local/` synthetic origin; emulator WebGPU adapter was unavailable and physical/target-device coverage remains open
- Android Emulator dynamic VSR working-set candidate: a freshly generated embedded-payload APK on `Rcl_Aether_API35_ATD` verified 4 cells/33 ready payloads/11 bindings → empty far working set with 33 releases and 33 evictions → one-cell re-entry with 12 ready payloads/4 bindings; all phases had `importFailed=0`, empty errors, and `verified=true`; this is Emulator/WebView evidence only
- Dynamic VSR working-set candidate: implemented in the Reality Build browser host through the existing `resolveSpatialStreaming()` and `VSRSpatialAssetStreamer.reconcile()` seams; after a real Chromium reset, observer transition reduced 4 cells/12 bindings to 1 cell/6 bindings with `released=36` and `evicted=18`, then re-entry reloaded `8576` bytes and restored 3 bindings; all three frames were WebGPU `verified=true` with zero import/missing/failed assets; the same seam now has an embedded Android Emulator candidate with 33 release/eviction and 12-payload re-entry evidence; physical/target-device GPU, persistent cache curves, newly generated Large World chunks, network, and production asset service remain open
- Network compilation → headless candidate: implemented by reusing Reality Studio `compileNetworkWorld()` and `verifyNetworkWorldCompilation()`; Build identity/graph/receipt/integrity retain `network_compilation_root`, generated headless servers carry the same artifact, old `/network/*` loopback endpoints remain available, and `/network/authority/*` accepts the existing `network.input.v0.2` packet. The reusable `HttpAuthorityClient` builds on `ClientPredictionRuntime`, performs compiled-slot join, packet submission, tick ack/delta reconciliation, health and metrics; the integration test starts the generated server, joins loopback slots, runs that separate HTTP authority client through delegation/input/ack convergence, verifies source roots/client synchronization, and rejects tampered/missing network artifacts. This is local deterministic candidate evidence only, not WAN, multi-device, cross-node security, load, or production deployment proof
- Browser CacheStorage candidate provider: implemented through the existing VSR loader seam; a real Chromium run verified 39 first-load misses → 39 cold-reload hits, then a changed payload revision emitted `VSR_BROWSER_ASSET_CACHE_REVISION_CHANGED`, rebuilt 36 entries, and reached 36 cold-reload hits with `importFailed=0`; Android app-private persistence, quota/performance and cross-process/remote coherence remain open in this browser-only line
- Android WebView CacheStorage lowering: implemented in the generated `android-project` host through the same VSR provider contract; `loadDataWithBaseURL("https://rncs.local/", ...)` avoids the pre-change `file://` CacheStorage rejection, and a real API 35 emulator run verified v1 `39` misses/`170656` bytes, v2 revision invalidation with `36` misses/`156064` bytes, then process-restart rehydrate with `36` hits and `importFailed=0`; custom native cache bridge is not required for this candidate, while quota/eviction/performance, physical devices and remote coherence remain open. Evidence: `evidence/ANDROID_ASSET_CACHE_PROVIDER_v0.1.json`
- Android WebView bounded CacheStorage candidate: `asset_cache.max_bytes` is now explicit in the Reality Build request/schema and build identity, and cached asset ids provide a VSR load-order hint without changing the VSR resolution root; a real API 35 emulator run under a `50,000`-byte provider budget verified v1 process-restart rehydrate (`8` hits, `49,524` resident bytes, `28` evictions), v2 revision invalidation (`47,496` resident bytes), and v2 process-restart rehydrate (`8` hits, `46,980` resident bytes), with `ready=39/36`, `importFailed=0`, and final resident bytes within budget. Quota guarantees, performance curves, physical devices and remote coherence remain open. Evidence: `evidence/ANDROID_ASSET_CACHE_BUDGET_CANDIDATE_v0.1.json`
- Android WebView cache metadata/performance candidate: `asset_cache.persist_accesses` is now explicit in the request/schema and Build identity; Build defaults it to `false`, payload writes still await a durable coalesced manifest flush, and VSR cached-id scheduling remains the shared load-order hint. On the same API 35 ATD profile, a concurrent 4×16 KB control measured `772.3/799.2 ms` for direct CacheStorage write/read and `2,182.4/1,856.8 ms` for the content-addressed provider (including verification and manifest bookkeeping); 8/32/128 KB half-capacity probes each reached two resident payloads and two evictions. Current v1/v2 cold, restart and revision phases remained `ready=39/36`, `importFailed=0`, with resident bytes `45,640/49,524/47,496/46,980`. This is an emulator/WebView profile candidate, not a quota guarantee, physical-device curve or remote coherence result. Evidence: `evidence/ANDROID_ASSET_CACHE_PERFORMANCE_CANDIDATE_v0.1.json`
- Reality Studio UI/Input target lowering candidate: Build now validates and lowers the existing reality-studio.ui-tree.v1.2 and reality-studio.input-profile.v1.2 contract into web-release, web-single and embedded Android targets. A source-generated fixture produced a sealed 13-node UI runtime and 12-action input profile; real Chromium exposed 13 nodes/7 desktop-visible nodes, KeyD drove Behavior state x=70 → 166, and KeyP made the Studio pause panel visible (visible=9) with no application error. The same debug APK ran on Rcl_Aether_API35_ATD; WebView exposed 13 nodes/11 touch-visible nodes, and a real adb shell input tap 366 924 reached ui:right → move_right, moving x=70 → 73.2. Malformed partial UI/Input sections fail closed and projects without the sections retain the legacy fallback. This is target-lowering candidate evidence, not native GPU UI, physical-device, accessibility/localization, release or human visual acceptance. Evidence: evidence/BUILD_UI_INPUT_TARGET_LOWERING_CANDIDATE_v0.1.json
- Reality Build animation target lowering candidate: existing VSR clip sampling, skinning, morph and glTF import semantics now cross the explicit Build candidate seam through a sealed fixed-tick animation policy. Deformed replace-mesh imports preserve one bound render node, imported skins/animations and support joint nodes, while ambiguous or non-identity deformed roots fail closed. The generated web-release and Android embedded HTML carry the same policy; real Chromium/WebGPU verified Tick 0 → 15, animation time 0 → 0.25 seconds, changing animationRoot, `verified=true` and 0 application errors. The Android result is source-project HTML evidence only; graph/layer/Sequencer lowering, APK/runtime, physical/native GPU, performance and human visual acceptance remain open. Evidence: evidence/BUILD_ANIMATION_TARGET_LOWERING_CANDIDATE_v0.1.json
- Audio target lowering archaeology: the source `sfx-wav` is real and is content-addressed into the web-release target, but the runtime map exposes only `primary_visual`; Behavior `experience.audio.emit` cues have no explicit asset binding, and RSR `spatial-audio` events are not forwarded to a target audio consumer. A clean Chromium source build entered the procedural oscillator fallback while making zero WAV requests and observing no `decodeAudioData` or audio-file receipt. This is negative candidate evidence only, not audio playback, Android audio, latency, mixing, production mastering or human listening proof. Gap: `RCL_GAP_RNCS_AUDIO_TARGET_LOWERING`; Evidence: evidence/BUILD_AUDIO_TARGET_LOWERING_AUDIT_v0.1.json
- Release-signed APK/AAB: not implemented
