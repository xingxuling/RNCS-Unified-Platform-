# Reality Studio v1.3 测试报告

## 自动化测试

- 原有 Studio 测试：138项回归；
- 新增资产连续性测试：18项；
- 合计：156项；
- VSR vendored测试：120项，仍由`npm run test:vsr`独立执行。

## 新增覆盖

- MIME、资产类型与角色识别；
- 本地文件导入与内容寻址；
- 稳定身份和代际推进；
- JSON依赖发现；
- 目录递归扫描与忽略规则；
- 未变化与变化重导入；
- 缺失源软失败；
- 依赖图解析与未解析依赖；
- 陈旧源和严格缺失审计；
- 连续性账本；
- Studio会话导入、全量重导入和导出；
- 服务端嵌入资产导入、审计、依赖图和账本；
- 离线浏览器真实文件导入。

## 浏览器验收

离线工作台从4个资产导入到5个资产，资产卡片、审计摘要和导入/重导入/审计按钮均可见，页面错误为0。

## 基准边界

250个临时本地文件的导入、依赖图、审计与账本基准仅代表当前容器文件系统，不外推到用户硬件或大型项目。
