# DML Core v0.3 能力闭环

运行时不再假设所有能力都存在。每个目标先产生能力计划，并把能力映射到真实 Provider。

能力状态分为：可用、不可用、已发现但未授权。代码智能体的“发现”和“授权”必须分开。

失败分为：临时故障、能力缺口、权限缺口、验证失败和普通 Provider 失败。只有临时故障进入有限退避重试；其余失败会保留证据并阻塞或重新规划。

VSR 阴影稳定任务由内置专用 Provider 执行。其他代码任务需要通用 Agent Provider；缺少时明确失败。
