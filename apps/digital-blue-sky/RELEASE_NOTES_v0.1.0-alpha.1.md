# DML Core Runtime v0.1.0-alpha.1 发布说明

本版本第一次将数字蓝天机 Workbench 的按钮和工作状态连接到真实能力协议与事件运行时。

关键变化不是增加传统后端接口，而是冻结：

```text
界面表达目标
→ 能力协商
→ 权威裁决
→ 执行
→ 事件
→ 投影
```

这证明“API 被降级为 Provider 内部实现”在 DML Workbench 中可以落地。
