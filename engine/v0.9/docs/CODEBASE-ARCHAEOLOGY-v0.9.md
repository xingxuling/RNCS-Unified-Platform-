# Codebase Archaeology v0.9

v0.8 already contained authoritative frames, prediction replay, OBB SAT, CCD, spatial hashing, traversal basics, PBR, glTF geometry/material import and temporal presentation. The minimum change surface was exact Capsule-OBB contact, friction persistence, deterministic islands and full PBR texture projection. Network was deliberately not version-bumped because it continued to transport the same RSR authority protocol.
