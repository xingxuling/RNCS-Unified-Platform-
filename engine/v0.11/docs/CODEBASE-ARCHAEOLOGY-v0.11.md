# Codebase Archaeology v0.11

The existing behavior runtime already had deterministic events, rules, state machines, behavior trees, authority calls, snapshots, breakpoints and hot reload. The missing production surface was explicit quest/task semantics, replay receipts, and a Studio timeline that orchestrates authority and presentation tracks without conflating their roots. Therefore v0.11 extends rather than replaces the previous runtime.
