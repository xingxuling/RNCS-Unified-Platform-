# Code Review v0.10

Resolved findings:

- Studio only accepted RAGF v0.1 continuity bundles; explicit v0.1/v0.2/v0.3 compatibility is now tested.
- Non-recommended candidates now rebuild bundle, family, lineage, Studio import and authority request from the selected candidate.
- Gateway cleans optional undefined fields before deterministic canonical serialization.
- Float-heavy preview meshes are runtime-only; sealed manifests contain roots, counts and integer millimeter values.

Boundary: built-in generators are deterministic reference producers, not film-grade topology, facial rigs, cloth, hair or motion-capture systems.
