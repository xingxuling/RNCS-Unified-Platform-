import fs from 'node:fs';import path from 'node:path';import {AetherworldRNCSNativeRuntime} from '@taowind/aether-rncs-bridge';
const out=path.resolve('artifacts/aether-island-native-runtime');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});const runtime=new AetherworldRNCSNativeRuntime({dataDir:path.join(out,'runtime')});const result=await runtime.runEndToEnd();fs.writeFileSync(path.join(out,'e2e-evidence.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({status:Object.values(result.acceptance).every(Boolean)?'PASS':'FAIL',acceptance:result.acceptance,evidence_root:result.evidence_root,artifact:path.join(out,'e2e-evidence.json')},null,2));if(!Object.values(result.acceptance).every(Boolean))process.exitCode=1;
