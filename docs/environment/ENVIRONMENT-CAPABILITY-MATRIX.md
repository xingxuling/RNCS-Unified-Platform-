# 环境能力矩阵

| 能力 | ChatGPT 客户端 | TaoWind MCP v0.2 | RNCS/Aetherworld 运行时 | 本次离线交付环境 |
|---|---:|---:|---:|---:|
| 读取服务器/运行时状态 | 调用 | 暴露 | 执行 | 已验证 |
| 候选编译、创建、模拟 | 调用 | 编排 | 执行 | 已验证 |
| AAF 授权 | 调用 | 暴露 | 执行 | 已验证 |
| 合并正式世界 | 调用 | 前置条件＋编排 | 执行 | 已验证 |
| 正式 Loopback | 调用 | 前置条件＋编排 | 执行 | 已验证 |
| Generation 回滚/重放 | 调用 | 前置条件＋编排 | 执行 | 测试覆盖 |
| 任意已声明运行时动作 | 调用 | Manifest 白名单 | 执行 | 已验证 |
| 任意 Shell | 不开放 | 不开放 | 无协议 | 不开放 |
| 任意文件访问 | 不开放 | 不开放 | 无协议 | 不开放 |
| 发布到公网 | 无部署凭据 | 只能作为待部署服务 | 不负责托管 | 未执行 |
| 修改 GitHub/Vercel 在线实例 | 当前无写连接器 | 不具备自部署动作 | 不负责 | 未执行 |

## 结构裁决

这次升级已经把“只能看”改成“能够改变正式现实”，但正式写入仍走 RNCS 原生权威链，而不是绕开它。对单用户系统而言，真正应取消的是多余的人类审批摩擦；不应取消的是状态并发检查、证据和恢复能力。
