# RNCS Anime Forge Phase 6.5 — Federation Review

## Founder Twin
The user-rejected Phase 6.4 playback frames are the governing reality residual. The goal is not another successful MP4; it is a character drawing stage that produces a readable Anime human form from validated morphology.

## 柳清莲 Gate
Do not hide visual failure behind green geometry or semantic certificates. Human visual acceptance remains an independent final gate.

## 洞哥 Grounding
The observed failure is downstream of Morphology/Visibility: the final drawing path still encoded torso and limbs as primitive shapes. Therefore the change must sit between validated semantic anatomy and rasterization.

## Product / UX
Reality value is a visible character-quality uplift. The engine must keep deterministic offline CPU execution and preserve inspectable roots.

## Character Design / Art Director
Required drawing semantics: head construction, face perspective, scalp-bound hair silhouette, shoulder socket, armpit, ribcage-waist-pelvis hierarchy, tapered upper/forearm, readable palm, garment collar/sleeve/body contour.

## Animation Director
The drawing model must remain stable in front, 3/4 and profile views and under the existing action pose; it must not collapse back to a front-facing puppet when joints move.

## Technical Art / Graphics
Selected architecture: a `CharacterDrawingCompiler` deep module downstream of NativeSurfaceFrame. It compiles validated 2.5D/3D state into view-conditioned 2D contour rigs. The rasterizer only consumes the compiled drawing model.

## Alternative rejected
1. Continue tuning polygon torso + capsule limbs: rejected because it preserves the representation ceiling that caused the human failure.
2. Rebuild Canonical Morphology again: rejected because Phase 6.3/6.4 geometry and semantic authority are already useful and are not the current primary residual.
3. External image/video model: rejected as mainline acceptance path because the project is pursuing native visual genesis.

## Engineering Civilization
Vertical slices:
1. RED baseline from Phase 6.4 primitive projection.
2. CharacterDrawingCompiler + measured DrawingCertificate.
3. Renderer migration: no final `capsule()` body path.
4. Static Front/3Q/Profile gates.
5. 120-frame native candidate + FFmpeg/ffprobe.
6. Human review remains pending.

## Test Civilization
Core drawing gates are measured/derived properties, not naked `true` declarations. A legacy Phase 6.4 projection must remain distinguishable from the new drawing model.

## Safety / Release
No external model weights, API secrets, or binary dependencies are added. Existing geometry authority remains intact. The branch can be reverted without invalidating Phase 6.4.

## Integration Court
Acceptance for merge is engineering-only: compiler, contour rig, certificates, static gates, 5-second candidate and CI must pass. `human_visual_acceptance` and `creative_production_review` must stay pending until the user sees the actual output.

## Evidence Ledger
The ledger must bind Character Genome identity root, semantic morphology root, Human FAIL review root, static drawing gate root, frame manifest root and final media hash.
