# @vsr/cli

校验、检查、求值、渲染、视频编码、事务补丁、确定性验证、Studio 静态服务和基准测试。

## Alpha.7 Shared Reality

```bash
vsr serve-shared <document> --session shared:demo --port 4180
vsr rfe-journal <commit-request.json> --journal outputs/rfe-journal.json
vsr schema --kind shared-event
vsr schema --kind shared-candidate
vsr schema --kind shared-revocation
vsr schema --kind rfe-commit-request
```
