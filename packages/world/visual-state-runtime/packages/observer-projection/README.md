# @vsr/observer-projection

Observer-relative projection fabric for VSR Visual IR.

It applies node-level policies from `extensions["vsr:observer-policy"]` and produces a deterministic observer projection manifest with show, redact and hide decisions.
