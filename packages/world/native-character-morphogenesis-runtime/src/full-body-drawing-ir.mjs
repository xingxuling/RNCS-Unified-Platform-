import {seal} from './canonical.mjs';
import {compileAnimeDrawingIR,validateAnimeDrawingIR,drawingIrToSvg} from './drawing-ir.mjs';
import {compileFullBodyCharacterDrawing,validateFullBodyCharacterDrawing} from './full-body-drawing.mjs';
import {buildMeshSilhouetteDrawingGuide,validateMeshSilhouetteDrawingGuide} from './mesh-silhouette-drawing.mjs';

const FORMAT='rncs.anime-drawing-ir.v0.1';
const round=value=>Number(Number(value).toFixed(3));
function smoothClosed(points,tension=.76){if(!Array.isArray(points)||points.length<3)return'';const n=points.length,out=[`M ${round(points[0][0])} ${round(points[0][1])}`];for(let i=0;i<n;i++){const p0=points[(i-1+n)%n],p1=points[i],p2=points[(i+1)%n],p3=points[(i+2)%n],c1=[p1[0]+(p2[0]-p0[0])*tension/6,p1[1]+(p2[1]-p0[1])*tension/6],c2=[p2[0]-(p3[0]-p1[0])*tension/6,p2[1]-(p3[1]-p1[1])*tension/6];out.push(`C ${round(c1[0])} ${round(c1[1])} ${round(c2[0])} ${round(c2[1])} ${round(p2[0])} ${round(p2[1])}`);}out.push('Z');return out.join(' ');}
const path=(id,points,{fill,stroke,stroke_width,z,role,tension=.76,meshGuideRoot=null}={})=>({kind:'path',id,role,z,d:smoothClosed(points,tension),fill,stroke,stroke_width,line_join:'round',line_cap:'round',...(meshGuideRoot?{mesh_silhouette_guide_root:meshGuideRoot}: {})});

function replaceMeshDrivenBodyOperations(operations,guides,palette){return operations.map(op=>{if(op.id==='garment-body'&&guides?.torso)return path(op.id,guides.torso.contour,{fill:op.fill??palette.coat,stroke:op.stroke??palette.ink,stroke_width:op.stroke_width??2.4,z:op.z,role:op.role,tension:.88,meshGuideRoot:guides.torso.guide_root});if(op.id==='sleeve-left'&&guides?.['arm-left'])return path(op.id,guides['arm-left'].contour,{fill:op.fill??palette.coat,stroke:op.stroke??palette.ink,stroke_width:op.stroke_width??2.1,z:op.z,role:op.role,tension:.90,meshGuideRoot:guides['arm-left'].guide_root});if(op.id==='sleeve-right'&&guides?.['arm-right'])return path(op.id,guides['arm-right'].contour,{fill:op.fill??palette.coat,stroke:op.stroke??palette.ink,stroke_width:op.stroke_width??2.1,z:op.z,role:op.role,tension:.90,meshGuideRoot:guides['arm-right'].guide_root});return op;});}

export function compileFullBodyAnimeDrawingIR(frame,{palette={},useMeshSilhouette=null,meshSilhouetteOptions={}}={}){
  const base=compileAnimeDrawingIR(frame,{palette}),drawing=compileFullBodyCharacterDrawing(frame),drawingValidation=validateFullBodyCharacterDrawing(drawing);if(!drawingValidation.valid)throw Object.assign(new Error(`FULL_BODY_DRAWING_IR_SOURCE_REJECTED:${drawingValidation.errors.join(',')}`),{code:'FULL_BODY_DRAWING_IR_SOURCE_REJECTED',drawingValidation});
  const meshSilhouetteEnabled=useMeshSilhouette??Boolean(frame?.surface_weighting_root);let silhouette=null,operations=[...base.operations];
  if(meshSilhouetteEnabled){silhouette=buildMeshSilhouetteDrawingGuide(frame,meshSilhouetteOptions);const validation=validateMeshSilhouetteDrawingGuide(silhouette,{requireFullBody:true});if(!validation.valid)throw Object.assign(new Error(`FULL_BODY_MESH_SILHOUETTE_REJECTED:${validation.errors.join(',')}`),{code:'FULL_BODY_MESH_SILHOUETTE_REJECTED',validation,silhouette});operations=replaceMeshDrivenBodyOperations(operations,silhouette.guides,base.palette);}
  const p=base.palette,extra=[];for(const leg of drawing.lower_body.legs){const z=leg.depth_order==='far'?7:18,legGuide=silhouette?.guides?.[`leg-${leg.side}`],footGuide=silhouette?.guides?.[`foot-${leg.side}`],legContour=legGuide?.contour??leg.full_contour,footContour=footGuide?.contour??leg.foot.contour;extra.push(path(`leg-${leg.side}`,legContour,{fill:p.coat,stroke:p.ink,stroke_width:2.1,z,role:'continuous-leg-garment',tension:.88,meshGuideRoot:legGuide?.guide_root??null}));extra.push(path(`foot-${leg.side}`,footContour,{fill:p.coat_dark,stroke:p.ink,stroke_width:1.8,z:z+.3,role:'foot-silhouette',tension:.6,meshGuideRoot:footGuide?.guide_root??null}));}
  const data={...base,format:FORMAT,version:'0.3.0-alpha.1',drawing_root:drawing.drawing_root,upper_body_drawing_certificate_root:drawing.drawing_certificate_root,full_body_drawing_certificate_root:drawing.full_body_drawing_certificate_root,lower_body_status:drawing.lower_body_status,mesh_silhouette_guide_root:silhouette?.guide_set_root??null,mesh_silhouette_policy:meshSilhouetteEnabled?'weighted-posed-mesh-visible-surface':'disabled-compatible',operations:[...operations,...extra].sort((a,b)=>a.z-b.z),drawing_ir_root:''};return seal(data,'drawing_ir_root');
}
export function validateFullBodyAnimeDrawingIR(ir,{requireMeshSilhouette=false}={}){const base=validateAnimeDrawingIR(ir),errors=[...base.errors],legOps=(ir?.operations??[]).filter(op=>op.role==='continuous-leg-garment'),footOps=(ir?.operations??[]).filter(op=>op.role==='foot-silhouette'),sleeveOps=(ir?.operations??[]).filter(op=>op.role==='continuous-sleeve'),garmentOps=(ir?.operations??[]).filter(op=>op.role==='garment-body-silhouette');if(ir?.lower_body_status!=='represented-in-canonical-morphology-v0.2-candidate')errors.push('FULL_BODY_DRAWING_IR_STATUS_INVALID');if(legOps.length!==2)errors.push('FULL_BODY_DRAWING_IR_LEG_COUNT_INVALID');if(footOps.length!==2)errors.push('FULL_BODY_DRAWING_IR_FOOT_COUNT_INVALID');if(!ir?.full_body_drawing_certificate_root)errors.push('FULL_BODY_DRAWING_IR_CERTIFICATE_ROOT_MISSING');if(requireMeshSilhouette){if(!ir?.mesh_silhouette_guide_root)errors.push('FULL_BODY_DRAWING_IR_MESH_SILHOUETTE_ROOT_MISSING');for(const op of [...garmentOps,...sleeveOps,...legOps,...footOps])if(!op.mesh_silhouette_guide_root)errors.push(`FULL_BODY_DRAWING_IR_MESH_GUIDE_BINDING_MISSING:${op.id}`);}return{valid:errors.length===0,errors,drawing_ir_root:ir?.drawing_ir_root??null,mesh_silhouette_guide_root:ir?.mesh_silhouette_guide_root??null};}
export function fullBodyDrawingIrToSvg(ir,options={}){const validation=validateFullBodyAnimeDrawingIR(ir,{requireMeshSilhouette:Boolean(ir?.mesh_silhouette_guide_root)});if(!validation.valid)throw Object.assign(new Error(`FULL_BODY_DRAWING_IR_INVALID:${validation.errors.join(',')}`),{code:'FULL_BODY_DRAWING_IR_INVALID',validation});return drawingIrToSvg(ir,options);}
