import fs from 'node:fs';import path from 'node:path';import {AetherworldRNCSNativeRuntime} from '../src/index.mjs';
const out=path.resolve('packages/integration/aether-rncs-bridge/examples/output');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});const runtime=new AetherworldRNCSNativeRuntime({dataDir:path.join(out,'runtime')});const result=await runtime.runEndToEnd();fs.writeFileSync(path.join(out,'aether-island-evidence.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify({status:'PASS',acceptance:result.acceptance,evidence_root:result.evidence_root,output:path.join(out,'aether-island-evidence.json')},null,2));
