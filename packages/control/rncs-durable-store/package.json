{
  "name": "@taowind/rncs-durable-store",
  "version": "0.1.0-alpha.1",
  "type": "module",
  "description": "Node-only atomic JSON storage primitive for candidate RNCS artifacts; domain verification and authority remain with callers.",
  "license": "Apache-2.0",
  "exports": {
    ".": "./src/index.mjs"
  },
  "files": ["src", "tests", "README.md"],
  "scripts": {
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
