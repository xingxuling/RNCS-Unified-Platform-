import fs from 'node:fs'; import path from 'node:path'; import {performance} from 'node:perf_hooks'; import {negotiate} from '../src/negotiator.mjs';
const root=path.resolve(import.meta.dirname,'..'),load=p=>JSON.parse(fs.readFileSync(path.join(root,p),'utf8'));const req=load('examples/requests/rncs-video.request.json');const providers=fs.readdirSync(path.join(root,'examples/providers')).filter(x=>x.endsWith('.json')).sort().map(x=>load('examples/providers/'+x));const xs=[];for(let i=0;i<3000;i++){const t=performance.now();negotiate({request:req,providers});xs.push(performance.now()-t)}xs.sort((a,b)=>a-b);const out={iterations:xs.length,median_ms:xs[Math.floor(xs.length*.5)],p95_ms:xs[Math.floor(xs.length*.95)],min_ms:xs[0],max_ms:xs.at(-1)};console.log(JSON.stringify(out,null,2));
