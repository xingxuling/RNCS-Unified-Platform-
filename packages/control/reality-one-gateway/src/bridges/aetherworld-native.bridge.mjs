import path from 'node:path';import {AetherworldRNCSNativeRuntime} from '@taowind/aether-rncs-bridge';
export async function createBridge({manifest,dataDir}){const stateDir=dataDir?path.resolve(dataDir,'runtime-state','aetherworld-native'):path.resolve(path.dirname(manifest.manifest_file),'../output/aetherworld-native');const runtime=new AetherworldRNCSNativeRuntime({dataDir:stateDir});return{health:()=>runtime.health(),invoke:(action,payload)=>runtime.invoke(action,payload)};}
