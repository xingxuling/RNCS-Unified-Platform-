export async function createBridge({module}){return{health:()=>({status:'ok',protocol:'dml.cognitive-runtime.v0.5'}),invoke:async(action,payload)=>{if(action==='health')return{status:'ok',protocol:'dml.cognitive-runtime.v0.5'};if(action==='createRuntime')return{available:typeof module.DMLRuntime==='function',options:payload??{}};throw Object.assign(new Error(`Unsupported DML action: ${action}`),{code:'DML_ACTION_UNSUPPORTED'});}};}
