import fs from 'node:fs';import path from 'node:path';import {createHash} from 'node:crypto';
const root=path.resolve(path.dirname(new URL(import.meta.url).pathname),'..'),manifest=JSON.parse(fs.readFileSync(path.join(root,'release-manifest.json'),'utf8'));let verified=0;const bad=[];for(const f of manifest.files){const p=path.join(root,f.path);if(!fs.existsSync(p)){bad.push({path:f.path,error:'missing'});continue;}const b=fs.readFileSync(p),h=createHash('sha256').update(b).digest('hex');if(h!==f.sha256)bad.push({path:f.path,error:'hash',expected:f.sha256,actual:h});else verified++;}const report={status:bad.length?'FAIL':'PASS',verified,total:manifest.files.length,bad};console.log(JSON.stringify(report,null,2));if(bad.length)process.exitCode=1;
