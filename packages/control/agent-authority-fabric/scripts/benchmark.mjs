import fs from 'node:fs';import {performance} from 'node:perf_hooks';import {evaluateAuthority} from '../src/index.mjs';const x=JSON.parse(fs.readFileSync(new URL('../examples/evaluation-input.json',import.meta.url),'utf8'));const times=[];for(let i=0;i<3000;i++){const a=performance.now();evaluateAuthority(x);times.push(performance.now()-a)}times.sort((a,b)=>a-b);const out={iterations:3000,median_ms:times[1500],p95_ms:times[2850]};fs.writeFileSync(new URL('../evidence/BENCHMARK_v0.1.0.json',import.meta.url),JSON.stringify(out,null,2)+'\n');console.log(out);
