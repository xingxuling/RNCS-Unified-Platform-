// v0.9 示例 B：主体 + 主权阶段 + 阶段转移 + 编译层 + 信号 + 再生事件
export const V09_STAGES = `// ===== CSL v0.9 示例 B：主体 + 阶段 + 编译层 + 信号 + 再生事件 =====

// --- 主权阶段 ---
主权阶段 借权 { 序号 = 2, 关键词 = (借壳, 依附), 描述 = "依附他人框架" }
主权阶段 摄权 { 序号 = 6, 关键词 = (收权, 中枢), 描述 = "形成自身秩序中心" }
主权阶段 回权 { 序号 = 9, 关键词 = (回收, 内核回归), 描述 = "拉回定义权" }

// --- 阶段转移 ---
阶段转移 借到摄 { 从 借权 到 摄权 触发 边界感 > 50 }

// --- 主体 ---
主体 创业者A {
  当前阶段 = 借权
  边界感 = 75
}

// --- 编译层 ---
编译层 浮现层 {
  层级 = "B"
  输入 = (高密度结构云)
  输出 = (关键词闪现, 命名欲)
}

// --- 信号 ---
信号 命名冲动 { 类型 = "概念冲动", 强度 = 90, 描述 = "强烈想给某物起新名字" }

// --- 再生事件 ---
再生事件 Agent升级 {
  主体 = "创业者A"
  失配 = "旧策略在新环境失效"
  诊断 = "权限分配模型不匹配"
  重组 = "重写边界协议"
  新版本 = "v2.0"
}
`;
