# Reality Network Runtime v0.1.0-alpha.1

权威服务器固定 Tick、AAF delegation、RSR v0.6 世界执行、客户端预测与重演、确定性 Loopback 故障注入、RFE 网络证据、RBF 恢复候选和 Gateway 调用。

```bash
npm test --workspace @taowind/reality-network-runtime
npm run benchmark --workspace @taowind/reality-network-runtime
npm run demo:network
```
