# Software Architecture

The package separates protocol, authority, transport, server, client prediction, snapshot buffering, runtime orchestration and the RSR scene fixture. Dependencies point inward to public RNCS contracts. Network logic is not embedded in RSR or VSR.