# RNCS＋Aetherworld v0.18.2-alpha.1 开发验收报告

## 问题复现结论

v0.18.1 的启动诊断场景能够显示，但 `GameWorld._ready()` 同步创建完整地图、独立树木/岩石网格、敌人和NPC。主线程在初始化返回前无法刷新诊断界面，也没有等待上限，因此低配置设备会长期停留在“正在加载第三人称世界”。

## 修复

- Bootstrap改为线程化场景读取。
- 增加15秒场景读取上限与30秒世界初始化上限。
- 世界初始化拆分为出生区域、玩家、环境、敌人、NPC、存档六阶段。
- 每批实体创建后主动让出一帧。
- 90棵树与48块岩石改为3个低多边形MultiMesh批次。
- 启动页显示真实进度与当前阶段。
- 节点进入场景树之前使用本地position，避免无效global_transform访问。

## 验证

- GDScript语法解析：8/8 PASS。
- 启动架构检查：异步读取、双超时、阶段信号、MultiMesh、分批实体全部PASS。
- 原生Godot二进制运行：当前容器无法执行，仍需用户在Windows Godot 4.7 stable进行最终实机确认。

## 版本边界

本版优先解决启动与开放世界流送基础，不宣称已经完成商业级地图流送、LOD、导航网格或资源后台卸载。
